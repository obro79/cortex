# Cortex proof-page screenshots

These presenter-facing screenshots come from the local `/case-study` route.
They show a deterministic synthetic fixture, not provider-connected customer
data. Regenerate them while the local API is running on port 8010:

```bash
NODE_PATH=/path/to/node_modules node scripts/capture_hackathon_screenshots.mjs
```

Keep the visual fixture and **NOT LIVE** disclosure when using these in the
deck or demo video.
